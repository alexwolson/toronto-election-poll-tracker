[project]
name = "data-ingestion"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "pydantic",
    "requests",
]

[dependency-groups]
dev = [
    "pytest",
]
