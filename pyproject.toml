[project]
name = "data-ingestion"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.3",
    "openpyxl>=3.1.5",
    "pandas",
    "pydantic",
    "requests",
]

[dependency-groups]
dev = [
    "pytest",
]
