/**
 * One-line site notice under the masthead. Temporary editorial copy: shown
 * while the site runs on the release before the Ipsos poll of September 23,
 * and removed when that poll is re-added.
 */
export function SiteNotice() {
  return (
    <p className="site-notice" role="status">
      Update in progress: the Ipsos poll released September 23 is being added to the forecast.
    </p>
  );
}
